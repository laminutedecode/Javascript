// Fonction pour convertir une chaîne en nombre entier
function convertirEnNombre(chaine) {
    return parseInt(chaine, 10);
  }
  
  // Exemple d'utilisation
  const chaineExemple = "42";
  const resultat = convertirEnNombre(chaineExemple);
  console.log(resultat); // Output: 42
  