// Fonction pour calculer la puissance d'un nombre
function puissance(base, exposant) {
    return Math.pow(base, exposant);
  }
  
  // Exemple d'utilisation
  const base = 2;
  const exposant = 3;
  const resultat = puissance(base, exposant);
  console.log(resultat); // Output: 8
  